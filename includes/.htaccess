# InterLink — .htaccess
# Deny direct access to PHP includes (works on InfinityFree + Apache)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
